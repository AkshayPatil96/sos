version: '3.9'

services:
  api:
    build:
      context: .
      target: deps
    command: pnpm dev
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - '3000:3000'
      - '9229:9229'
    env_file: .env
    environment:
      NODE_ENV: development
      DATABASE_URL: postgresql://${DB_USER:-student_user}:${DB_PASSWORD:-student_pass}@postgres:5432/${DB_NAME:-student_db}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - app-network
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${DB_USER:-student_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-student_pass}
      POSTGRES_DB: ${DB_NAME:-student_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres-init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - '5432:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${DB_USER:-student_user} -d ${DB_NAME:-student_db}']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - app-network
    restart: unless-stopped

  # prometheus:
  #   image: prom/prometheus:v3.4.0
  #   command:
  #     - '--config.file=/etc/prometheus/prometheus.yml'
  #     - '--storage.tsdb.path=/prometheus'
  #     - '--storage.tsdb.retention.time=7d'
  #     - '--storage.tsdb.retention.size=512MB'
  #     - '--web.console.libraries=/usr/share/prometheus/console_libraries'
  #     - '--web.console.templates=/usr/share/prometheus/consoles'
  #   volumes:
  #     - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
  #     - prometheus_data:/prometheus
  #   ports:
  #     - '9090:9090'
  #   depends_on:
  #     - api
  #   networks:
  #     - app-network
  #   restart: unless-stopped

  # grafana:
  #   image: grafana/grafana:12.0.2
  #   volumes:
  #     - grafana_data:/var/lib/grafana
  #     - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
  #   environment:
  #     GF_SECURITY_ADMIN_USER: ${GRAFANA_USER:-admin}
  #     GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:-admin}
  #     GF_USERS_ALLOW_SIGN_UP: 'false'
  #     GF_AUTH_ANONYMOUS_ENABLED: 'true'
  #     GF_AUTH_ANONYMOUS_ORG_ROLE: 'Viewer'
  #     GF_SERVER_ROOT_URL: 'http://localhost:3001'
  #   ports:
  #     - '3001:3000'
  #   depends_on:
  #     - prometheus
  #   networks:
  #     - app-network
  #   restart: unless-stopped

volumes:
  postgres_data:
    driver: local
  # prometheus_data:
  #   driver: local
  # grafana_data:
  #   driver: local
  node_modules:
    driver: local

networks:
  app-network:
    driver: bridge
